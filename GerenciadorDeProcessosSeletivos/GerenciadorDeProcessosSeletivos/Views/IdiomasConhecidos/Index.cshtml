@model IEnumerable<GerenciadorDeProcessosSeletivos.Models.IdiomaConhecido>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nivel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nivel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdiomaConhecidoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdiomaConhecidoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdiomaConhecidoId })
        </td>
    </tr>
}

</table>
