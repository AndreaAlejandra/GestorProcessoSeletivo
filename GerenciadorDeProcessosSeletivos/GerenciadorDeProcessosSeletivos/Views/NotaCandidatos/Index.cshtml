@model IEnumerable<GerenciadorDeProcessosSeletivos.Models.NotaCandidato>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nota)
            @Html.DisplayNameFor(model => model.)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nota)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NotaCandidatoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.NotaCandidatoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NotaCandidatoId })
        </td>
    </tr>
}

</table>
